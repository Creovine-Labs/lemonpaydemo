"use client";

import { ComingSoonPage } from "@/components/ComingSoonPage";

export default function LimitsPage() {
  return (
    <ComingSoonPage
      subtitle="Settings"
      title="Limits"
      body="Daily and monthly limits + the increase-request flow ship in Phase 5."
      back="/app/settings"
    />
  );
}
