"use client";

import { ComingSoonPage } from "@/components/ComingSoonPage";

export default function KycSettingsPage() {
  return (
    <ComingSoonPage
      subtitle="Settings"
      title="Identity"
      body="KYC retry surface ships in Phase 5 alongside the onboarding stepper."
      back="/app/settings"
    />
  );
}
